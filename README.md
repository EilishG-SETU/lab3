# lab3
for excercise 1: after using the echo "alias myip='ip -4 addr show | grep -oP \"(?<=inet\s)\\d+(\\.\\d+){3}\"'" >> ~/.bashrc
command on the cli I used "source ~/.bashrc" to load the alias into the shell session and then rand the alias. 

